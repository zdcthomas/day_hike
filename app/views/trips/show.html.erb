<%= @trip.name %>
<%= @trip.total_trail_length %>
<%= @trip.average_trail_length %>
<%= @trip.max_trail_length %>
<%= @trip.min_trail_length %>


<% @trails.each do |trail| %>
  <%= trail.length %>
  <%= link_to  "#{trail.name}", trail_path(trail) %>
  <%= trail.address %>
<% end %>