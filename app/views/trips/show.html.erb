Name: <%= @trip.name %>
Total Length: <%= @trip.total_trail_length %>
Average Length: <%= @trip.average_trail_length %>
Maximum trail Length: <%= @trip.max_trail_length %>
Minimum trail Length: <%= @trip.min_trail_length %>


<% @trails.each do |trail| %>
  <%= link_to  "#{trail.name}", trail_path(trail) %>
  Length: <%= trail.length %>
  Address: <%= trail.address %>
<% end %>