Name: <%= @trail.name %>
Address: <%= @trail.address %>
Trip Count: <%= @trail.trip_count %>
<% @trips.each do |trip|%>
  Trip: <%= trip.name %>
  Total Length: <%= trip.total_trail_length %>
<% end %>